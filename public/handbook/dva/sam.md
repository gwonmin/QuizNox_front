# SAM (Serverless Application Model) 개념

**Lambda·API Gateway 등** 서버리스 리소스를 **템플릿**으로 정의하는 프레임워크입니다.

## 동작

- **YAML/JSON** 템플릿에 함수·API·이벤트 소스 정의. `sam deploy`로 배포.
- CloudFormation 기반.

## 용도

- 서버리스 앱을 코드로 관리, 로컬 테스트(sam local).

## 요약

- SAM = 서버리스 리소스 템플릿 + CLI. Lambda·API Gateway 등 선언적 정의.
