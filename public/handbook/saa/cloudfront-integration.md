# CloudFront 연동

**CDN**으로 캐시·배포를 앞단에 두는 패턴입니다.

## 동작

- **엣지**에서 정적·동적 콘텐츠 캐시, 오리진(S3, ALB, 커스텀)에서 미스 시 조회.
- HTTPS, 도메인·인증서 연결 가능.

## 연동

- S3 정적 호스팅 앞단에 CloudFront 두어 지연·부하 감소.
- ALB 오리진으로 동적 요청 라우팅.

## 요약

- CloudFront = 엣지 캐시 + 오리진(S3/ALB 등) 연동. 지연·트래픽 절감.
