# S3 정적 호스팅

**정적 웹 사이트**를 S3 버킷에 호스팅하는 방식입니다.

## 설정

- 버킷을 **정적 웹 사이트 호스팅**으로 설정, 인덱스·에러 문서 지정.
- 퍼블릭 읽기 또는 CloudFront로 배포.

## 제한

- 정적 파일만(HTML, CSS, JS, 이미지). 서버 사이드 로직은 Lambda@Edge 등과 조합.

## 요약

- S3 정적 호스팅 = 버킷에 정적 파일 올리고 웹으로 서빙. 동적 처리는 별도 서비스.
