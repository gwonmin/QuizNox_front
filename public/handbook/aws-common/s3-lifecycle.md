# S3 Lifecycle

**객체 나이·접두사**에 따라 **전환·만료**를 자동 적용하는 규칙입니다.

## 전환(Transition)

- 일정 일수 후 **다른 스토리지 클래스**로 이동 (예: Standard → IA → Glacier)
- 비용 절감용

## 만료(Expiration)

- 일정 일수 후 **객체 삭제** 또는 **이전 버전 삭제**
- 로그·임시 데이터 정리

## 요약

- Lifecycle = “N일 지나면 전환/만료” 규칙
- 버킷·접두사 단위로 설정
