# Sticky Session (ALB)

**같은 클라이언트 요청**을 **같은 대상**으로 보내도록 하는 기능입니다.

## 동작

- 쿠키(ALB 생성 또는 앱 생성)로 클라이언트를 식별
- 동일 클라이언트의 다음 요청을 이전과 같은 타깃으로 라우팅

## 용도

- **세션을 메모리에 두는** 앱에서 같은 인스턴스로 유지할 때
- Stateful 동작이 필요할 때

## 요약

- Sticky Session = 클라이언트별로 동일 타깃 유지
- 쿠키 기반, 세션 유지 필요 시 사용
