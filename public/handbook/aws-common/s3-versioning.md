# S3 Versioning

같은 **키에 여러 버전**을 두고 이전 버전을 보존·복구하는 기능입니다.

## 동작

- 버전링 활성화 시 객체 **덮어쓰기·삭제**해도 이전 버전 ID로 조회·복구 가능
- 삭제 시 삭제 마커만 붙고 버전은 유지(복구 가능)

## 용도

- 실수 덮어쓰기·삭제 복구, 규정 준수(이력 보존)

## 요약

- Versioning ON → 동일 키에 여러 버전 보존, 이전 버전 ID로 읽기·복구
