# CloudWatch Metric Filter

**로그에서** 패턴 매칭 후 **메트릭**을 만드는 기능입니다.

## 동작

- **로그 그룹**에 Metric Filter 정의. 패턴(예: ERROR) 매칭 시 카운트 등 메트릭 발생.
- 해당 메트릭으로 **알람** 설정 가능.

## 용도

- 로그 기반 지표(에러 수, 특정 문자열 출현 횟수) → 알람.

## 요약

- Metric Filter = 로그 패턴 → 메트릭. 로그 기반 알람에 사용.
