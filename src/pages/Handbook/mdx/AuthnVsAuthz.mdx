import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Authentication은 “누구인지”를 확인하는 단계이고, Authorization은 “무엇을 할 수 있는지”를 결정하는 단계입니다."
  bullets={[
    "Authentication: 로그인으로 주체를 식별한다.",
    "Authorization: 식별된 주체에게 허용/거부 범위를 결정한다.",
  ]}
/>

## Authentication vs Authorization 한눈에 비교

<KeyTable
  columns={["구분", "Authentication", "Authorization"]}
  rows={[
    [
      "질문",
      "당신이 누구인가?",
      "이 주체가 무엇을 할 수 있는가?",
    ],
    [
      "역할",
      "아이디/비밀번호, MFA, 토큰 등으로 신원을 확인",
      "역할·정책에 따라 리소스 접근과 동작을 허용/거부",
    ],
    [
      "수행 순서",
      "항상 먼저 수행된다.",
      "인증 후에 수행된다.",
    ],
  ]}
/>

## 요청 처리 흐름

<MermaidDiagram
  code={`sequenceDiagram
  participant U as User
  participant S as Service
  U->>S: 자원 요청
  S->>S: Authentication(누구인지 확인)
  S-->>U: 실패 시 거부
  S->>S: Authorization(무엇을 할 수 있는지 확인)
  S-->>U: 허용 또는 거부 응답`}
/>

