import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

**AWS Certified SysOps Administrator - Associate (SOA-C02)** 시험은 AWS의 **운영·모니터링·보안·네트워킹·자동화**에 중점을 두며, SAA보다 **런타임 운영과 문제 해결(Troubleshooting)** 비중이 높습니다. "오류 발생 시 해결 방법", CLI·콘솔 위치, 백업·복원 등 **실무 작업**을 묻는 문제가 많습니다.  
아래 노드를 클릭하면 해당 개념 문서로 이동합니다.

---

## SOA-C02 시험 구조 (6개 도메인)

<KeyTable
  columns={["도메인", "비중", "핵심 주제"]}
  rows={[
    ["모니터링, 로깅 및 복구", "20%", "CloudWatch(지표·대시보드·Logs Agent·알람·EC2 자동 복구), AWS Config(변경 추적·규정 준수), CloudTrail(API 감사)"],
    ["네트워킹 및 콘텐츠 전송", "18%", "VPC(서브넷·라우팅·IGW), SG vs NACL(Stateful vs Stateless), Route 53(레코드·라우팅 정책), CloudFront(원본·캐시 무효화)"],
    ["배포, 프로비저닝 및 자동화", "18%", "CloudFormation(스택·StackSets), SSM(Patch Manager·Run Command·Parameter Store), EC2 Image Builder"],
    ["고가용성 및 복구", "16%", "Auto Scaling(헬스체크·스케일 정책·웜업), ELB(ALB/NLB 트러블슈팅), RDS Multi-AZ·Read Replica, S3 Lifecycle·백업"],
    ["보안 및 컴플라이언스", "16%", "IAM(최소 권한·역할·정책), KMS(EBS·S3·RDS 암호화), AWS Shield·WAF"],
    ["비용 및 성능 최적화", "12%", "Cost Explorer·Budgets, EC2 Rightsizing(Compute Optimizer), S3 Intelligent-Tiering·EBS 유형 변경"],
  ]}
/>

---

## 1. 모니터링, 로깅 및 복구 (20%)

<MermaidDiagram
  code={`flowchart TB
  subgraph monitor [모니터링 · 로깅]
    MF([Metric Filter])
    CA([Composite Alarm])
    LI([Logs Insights])
  end
  MF --> CA --> LI`}
/>

---

## 2. 네트워킹 및 콘텐츠 전송 (18%)

<MermaidDiagram
  code={`flowchart TB
  subgraph network [네트워킹 · 트러블슈팅]
    FL([Flow Logs])
    DN([DNS 이슈])
    SG([SG/NACL 분석])
  end
  FL --> DN --> SG`}
/>

---

## 3. 배포, 프로비저닝 및 자동화 (18%)

<MermaidDiagram
  code={`flowchart TB
  subgraph deploy [배포 · 자동화]
    SM([Session Manager])
    PM([Patch Manager])
    RC([Run Command])
  end
  subgraph ec2op [EC2 운영]
    LT([Launch Template])
    AM([AMI 관리])
  end
  SM --> PM --> RC
  LT --> AM`}
/>

---

## 4. 고가용성 및 복구 (16%)

<MermaidDiagram
  code={`flowchart TB
  subgraph ha [고가용성 · 복구]
    ES([EBS 스냅샷])
  end
  ES`}
/>

---

## 5. 보안 및 컴플라이언스 (16%)

<MermaidDiagram
  code={`flowchart TB
  subgraph security [보안 · 컴플라이언스]
    AC([AWS Config])
    GD([GuardDuty])
  end
  AC --> GD`}
/>

---

## 6. 비용 및 성능 최적화 (12%)

<MermaidDiagram
  code={`flowchart TB
  subgraph cost [비용 최적화]
    CO([Cost Explorer / Budgets])
  end
  CO`}
/>

---

세부 설명은 각 개념 문서에서 이어서 읽을 수 있습니다.
