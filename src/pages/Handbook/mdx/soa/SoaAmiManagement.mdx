import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="Amazon Machine Image의 생성·복사·공유·암호화·리전/계정 간 사용 방법입니다."
  bullets={[
    "생성: 실행 중/중지된 인스턴스에서 AMI 생성 → 루트 볼륨이 EBS 스냅샷으로 저장. No Reboot 옵션으로 무중단 생성 가능",
    "리전 간 사용: AMI는 리전별. 다른 리전에서 쓰려면 복사(copy-image)",
    "암호화·공유: 커스텀 KMS 키로 암호화한 AMI를 다른 계정에 공유 시, 해당 계정에 KMS 키 정책으로 kms:Decrypt 등 허용 후 AMI 권한 공유. 복사 시 대상 계정 KMS 키로 재암호화 가능",
  ]}
/>

## 시나리오

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["한 AZ에 있는 EC2를 다른 AZ로 이전", "인스턴스에서 AMI 생성 → 다른 AZ(같은 리전)에서 해당 AMI로 인스턴스 시작 → 기존 인스턴스 종료"],
    ["us-east-1 템플릿을 eu-west-1에서 배포 시 실패", "AMI ID가 리전별로 다름. Mappings에 리전별 AMI ID, 리소스에서 Fn::FindInMap 사용"],
    ["암호화된 AMI를 다른 계정에 공유", "커스텀 KMS 키로 AMI 복사본 생성. KMS 키 정책에 대상 계정 역할/root 허용. AMI 권한에 계정 ID 추가"],
    ["다른 계정으로 복사한 암호화 AMI로 스택 실패", "대상 계정 KMS 키로 AMI를 재암호화해야 함"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["한 AZ EC2를 다른 AZ로 이전", "인스턴스에서 AMI 생성 → 다른 AZ에서 해당 AMI로 인스턴스 시작 → 기존 종료."],
    ["리전별 AMI ID(CloudFormation 다중 리전)", "Mappings에 리전→AMI. Fn::FindInMap으로 리소스에서 참조."],
    ["암호화 AMI 다른 계정 공유", "커스텀 KMS 키 정책에 대상 계정 허용. AMI 권한 공유. 복사 시 대상 계정 KMS로 재암호화."],
    ["다른 계정 복사한 암호화 AMI로 스택 실패", "대상 계정 KMS 키로 AMI 재암호화 후 사용."],
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "내용"]}
  rows={[
    ["생성", "인스턴스 → AMI 생성 → EBS 스냅샷 기반"],
    ["다중 리전", "AMI 복사(copy-image). CloudFormation은 Mappings로 리전별 AMI"],
  ]}
/>
