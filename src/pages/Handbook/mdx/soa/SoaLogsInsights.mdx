import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="저장된 CloudWatch 로그를 쿼리·집계·시각화하는 서비스입니다."
  bullets={[
    "쿼리 언어로 로그 그룹을 검색·필터·집계(stats, count, sum 등)",
    "여러 로그 그룹을 동시에 쿼리 가능(예: Lambda 함수 여러 개)",
    "저장된 로그 대상이므로 과거 장애 추적, 트렌드 분석, 특정 패턴 검색에 적합",
    "실시간 스트리밍이 아닌 배치 쿼리 방식",
  ]}
/>

## 시나리오

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["Lambda 함수가 최근 7일간 얼마나 에러 냈는지 파악", "Logs Insights에서 해당 함수 로그 그룹 대상으로 쿼리. 에러 패턴 필터 후 stats count"],
    ["여러 Lambda 로그 그룹에서 에러 타입별 개수", "Logs Insights 쿼리에서 stats … by (에러 타입 필드) + count"],
    ["VPC Flow Logs로 상위 5개 인터넷 목적지 분석", "Flow Log이 CloudWatch Logs에 있으면 Logs Insights로 집계"],
    ["과거 로그가 S3에만 있을 때", "S3에 있는 로그는 Athena 또는 Glue+Athena로 쿼리. Logs Insights는 CloudWatch Logs 전용"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["Lambda 함수 최근 7일 에러 횟수·패턴 파악", "Logs Insights에서 해당 로그 그룹 쿼리, 에러 패턴 필터 후 stats count."],
    ["여러 Lambda 로그 그룹에서 에러 타입별 개수", "stats … by (에러 타입 필드) + count."],
    ["Flow Logs 등 로그 집계·상위 N 분석", "Logs가 CloudWatch Logs에 있으면 Logs Insights로 쿼리·집계."],
    ["가장 운영 효율적인 로그 기반 에러 분석", "Logs Insights 쿼리(stats, count, filter)로 코드 없이 분석."],
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "내용"]}
  rows={[
    ["역할", "CloudWatch Logs 저장 로그의 쿼리·집계·시각화"],
    ["Metric Filter와 차이", "Metric Filter = 로그→메트릭 변환. Logs Insights = 로그 직접 쿼리·분석"],
  ]}
/>
