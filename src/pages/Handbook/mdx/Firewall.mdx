import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Firewall은 네트워크 경계에서 인바운드/아웃바운드 트래픽을 정책에 따라 허용하거나 차단하는 장치·기능입니다."
  bullets={[
    "인바운드: 외부에서 내부로 들어오는 트래픽 제어.",
    "아웃바운드: 내부에서 외부로 나가는 트래픽 제어.",
  ]}
/>

## 인바운드 vs 아웃바운드

<KeyTable
  columns={["방향", "의미", "예시 규칙"]}
  rows={[
    [
      "인바운드",
      "외부 → 내부로 들어오는 트래픽",
      "“80 포트만 허용”, “이 IP 대역만 허용”",
    ],
    [
      "아웃바운드",
      "내부 → 외부로 나가는 트래픽",
      "“HTTPS(443)만 허용”, “특정 API로만 허용”",
    ],
  ]}
/>

## 역할과 동작 방식

<KeyTable
  columns={["구분", "내용"]}
  rows={[
    [
      "역할",
      "정책에 따라 패킷·연결을 허용 또는 거부한다(IP, 포트, 프로토콜, 방향 등 기준).",
    ],
    [
      "Stateless",
      "패킷 단위로 규칙을 적용하며 연결 상태를 고려하지 않는다.",
    ],
    [
      "Stateful",
      "연결 상태를 보고 허용/차단하며, 응답 트래픽을 자동으로 허용할 수 있다.",
    ],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  T["트래픽"] --> F["Firewall"]
  F --> R{"규칙"}
  R -->|"허용"| A["통과"]
  R -->|"거부"| D["차단"]`}
/>

