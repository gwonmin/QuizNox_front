import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Container Registry는 컨테이너 이미지를 저장·버전 관리·배포하는 중앙 저장소입니다. CI에서 빌드된 이미지를 푸시하고, 노드·오케스트레이터는 필요한 이미지를 풀(Pull)해 실행한다."
  bullets={[
    "이미지는 레이어 단위로 저장되며, 푸시·풀 시 레이어 재사용으로 전송량과 저장 공간을 절약한다.",
    "레포지토리별 권한·네트워크 정책으로 어떤 서비스가 어떤 이미지를 가져갈 수 있는지 제어한다.",
    "공용(Docker Hub)과 사설(ECR, GCR, Harbor 등) 레지스트리를 구분해 보안·조직 정책을 반영한다.",
  ]}
/>

## Registry 핵심 개념

**한 줄 흐름:** 빌드 결과를 레지스트리에 푸시 → 레포지토리·태그로 버전 관리 → 노드·런타임이 인증 후 풀해 로컬 캐시에 두고 컨테이너 실행에 사용한다.

<KeyTable
  columns={["역할", "설명", "비고"]}
  rows={[
    ["저장", "빌드된 이미지를 레이어 단위로 저장해 여러 환경·노드에서 재사용할 수 있게 한다.", "OCI 이미지 스펙에 따른 매니페스트·레이어 구조."],
    ["배포", "노드·런타임이 컨테이너를 실행할 때 이미지를 풀하도록 제공한다. 풀 정책(Always, IfNotPresent 등)과 연동된다.", "네트워크·대역 제어로 풀 소스 제한 가능."],
    ["접근 제어", "레포지토리별 읽기·쓰기 권한, 네트워크 정책으로 승인된 주체만 푸시·풀하도록 한다.", "IAM, Robot 계정, Private Endpoint 등."],
  ]}
/>

## 푸시·풀 흐름

<MermaidDiagram
  code={`flowchart LR
  subgraph ci [CI 또는 로컬]
    build["이미지 빌드"]
    push["docker push"]
    build --> push
  end
  push --> reg["Registry"]
  subgraph node [노드]
    pull["PullImage"]
    run["컨테이너 실행"]
    pull --> run
  end
  reg --> pull`}
/>

## 공용 vs 사설 레지스트리

<KeyTable
  columns={["구분", "공용 레지스트리", "사설 레지스트리"]}
  rows={[
    ["예시", "Docker Hub(공개), Quay.io.", "AWS ECR, GCP GCR, Azure ACR, Harbor, Nexus."],
    ["용도", "공개 베이스 이미지·오픈소스 이미지 가져오기.", "조직 내부 이미지 저장·배포·정책 적용."],
    ["보안·정책", "레이트 리밋, 공개 범위 제한. 민감한 이미지는 비권장.", "VPC·Private Endpoint, 이미지 스캔, 생명주기 정책(미사용 이미지 삭제 등)."],
  ]}
/>

## Kubernetes와 레지스트리 연동

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["ImagePullSecrets", "레지스트리 인증 정보를 Secret으로 저장하고, Pod 스펙에서 imagePullSecrets로 참조해 private 레지스트리에서 풀한다."],
    ["ImagePullPolicy", "Always(항상 풀), IfNotPresent(없을 때만 풀), Never(로컬만). 기본값은 태그에 따라 다르며, :latest는 Always에 가깝게 동작할 수 있다."],
    ["노드 캐시", "한 번 풀한 이미지는 노드에 캐시되므로, 동일 이미지를 쓰는 Pod가 같은 노드에 있으면 풀을 생략할 수 있다."],
  ]}
/>

## 설계 시 유의점

<KeyTable
  columns={["항목", "권장 사항"]}
  rows={[
    ["인증·네트워크", "사설 레지스트리는 VPC 내부 또는 Private Link로 노드가 접근하게 하고, 인증은 서비스 계정·단기 토큰으로 최소 권한을 부여한다."],
    ["생명주기", "미사용 이미지·오래된 태그 정리 정책으로 저장 비용과 보안 노출을 줄인다. ECR Lifecycle Policy, Harbor 보존 규칙 등."],
    ["가용성", "레지스트리 장애 시 새 Pod 스케일아웃·배포가 실패할 수 있으므로, 다중 리전·백업 또는 캐시 레지스트리 구성을 검토한다."],
  ]}
/>

## 실제 예시

<KeyTable
  columns={["상황", "활용 예시"]}
  rows={[
    ["CI/CD 파이프라인", "빌드 단계에서 이미지를 ECR에 푸시하고, 배포 단계에서 Kubernetes Deployment의 image 필드를 해당 이미지 태그로 업데이트해 롤아웃한다."],
    ["다중 환경", "동일 이미지를 dev·staging·prod 레지스트리 또는 태그로 구분해 배포하고, 프로덕션만 스캔·승인 정책을 강하게 둔다."],
  ]}
/>
