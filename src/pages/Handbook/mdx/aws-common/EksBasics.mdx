import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="EKS(Elastic Kubernetes Service)는 관리형 Kubernetes 서비스입니다. AWS가 컨트롤 플레인(마스터)을 운영하고, 사용자는 워커 노드(EC2 또는 Fargate)를 붙여 표준 Kubernetes로 파드·서비스를 배포합니다."
  bullets={[
    "Kubernetes 호환: kubectl, Helm, YAML 매니페스트 그대로 사용. 관리형 컨트롤 플레인: 마스터 노드 패치·가용성은 AWS 책임.",
    "워커 노드: EC2(자체 관리) 또는 Fargate(서버리스) 선택. 노드 그룹으로 EC2 워커 묶음, ASG로 확장.",
    "용도: 이미 Kubernetes를 쓰는 워크로드 이전, 멀티 테넌트·마이크로서비스. ECS보다 K8s 생태계·표준이 필요할 때.",
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart TB
  CP[컨트롤 플레인 AWS 관리] --> W1[워커 노드]
  CP --> W2[워커 노드]
  W1 --> POD[파드]
  W2 --> POD`}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["EKS", "AWS 관리형 Kubernetes"],
    ["컨트롤 플레인", "AWS 관리, 패치·가용성 AWS 책임"],
    ["워커", "EC2 노드 그룹 또는 Fargate"],
    ["용도", "K8s 표준·생태계가 필요할 때"],
  ]}
/>
