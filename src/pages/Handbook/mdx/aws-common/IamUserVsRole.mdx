import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="IAM User는 한 사람·프로세스에 장기 자격을 주고, IAM Role은 임시로 맡아 쓰는 권한 단위입니다. 사람/CLI는 User 또는 Role(AssumeRole), AWS 서비스(EC2·Lambda 등)가 다른 서비스에 접근할 때는 Role을 쓰는 것이 권장됩니다."
  bullets={[
    "IAM User: 장기 자격 증명(Access Key, 비밀번호) 보유. 사람 로그인·CLI·CI 등에 사용.",
    "IAM Role: 자격 증명을 갖지 않음. AssumeRole로 임시 자격 발급. EC2 인스턴스 역할, Lambda 실행 역할 등 서비스 간·크로스 계정에 사용.",
    "서비스가 다른 AWS 리소스에 접근할 때는 Role 사용 권장.",
  ]}
/>

## User vs Role

<KeyTable
  columns={["구분", "User", "Role"]}
  rows={[
    ["자격", "장기(키·비밀번호)", "없음, Assume 시 임시 발급"],
    ["용도", "사람·CLI·고정 프로세스", "서비스 간·임시 위임·크로스 계정"],
    ["권장", "사람 접근", "서비스가 다른 AWS 리소스 접근 시 Role"],
  ]}
/>
