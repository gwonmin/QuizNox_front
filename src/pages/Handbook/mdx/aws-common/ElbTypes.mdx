import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="ALB는 L7(HTTP/HTTPS) 라우팅·헤더·경로 기반, NLB는 L4(TCP/UDP)로 고성능·고정 IP에 적합합니다. 클라이언트 요청을 여러 대상에 나누어 주는 로드 밸런서의 유형별 특성을 구분해 선택합니다."
  bullets={[
    "ALB: L7 HTTP/HTTPS, 호스트·경로·헤더 기반 라우팅. 타깃 EC2, IP, Lambda. 웹 앱·마이크로서비스·Lambda URL.",
    "NLB: L4 TCP·UDP·TLS, 초당 수백만 요청·초저지연. 고정 IP, 한 포트당 한 리스너. 게임·실시간 스트리밍·고정 IP 필요 시.",
    "CLB: 구형, 신규에는 ALB·NLB 권장.",
  ]}
/>

## ALB vs NLB

<KeyTable
  columns={["구분", "ALB", "NLB"]}
  rows={[
    ["계층", "L7 (HTTP/HTTPS)", "L4 (TCP/UDP)"],
    ["라우팅", "경로·호스트·헤더", "IP·포트"],
    ["용도", "웹·API·마이크로서비스", "고성능·고정 IP·초저지연"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  CLIENT[클라이언트] --> ALB[ALB L7]
  ALB --> T1[타깃 1]
  ALB --> T2[타깃 2]
  CLIENT --> NLB[NLB L4]
  NLB --> T3[타깃 1]
  NLB --> T4[타깃 2]`}
/>
