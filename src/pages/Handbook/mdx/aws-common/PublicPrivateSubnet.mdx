import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="VPC 내 서브넷은 인터넷과의 연결 방식으로 퍼블릭/프라이빗으로 구분합니다. 퍼블릭은 IGW로 직접 통신하고, 프라이빗은 NAT 등을 통해서만 아웃바운드가 가능합니다."
  bullets={[
    "퍼블릭 서브넷: 라우팅 테이블에 IGW(Internet Gateway) 경로가 있어 퍼블릭 IP 리소스가 인터넷과 직접 통신. 웹 서버·로드밸런서 배치.",
    "프라이빗 서브넷: IGW로 가는 기본 경로가 없고, 아웃바운드는 NAT Gateway 등으로만. DB·앱 서버 등 내부 리소스 배치.",
    "라우팅 테이블 설정(0.0.0.0/0 → IGW vs NAT)으로 퍼블릭/프라이빗 동작이 결정된다.",
  ]}
/>

## Public vs Private

<KeyTable
  columns={["구분", "Public", "Private"]}
  rows={[
    ["IGW 경로", "있음", "없음(또는 NAT 경유)"],
    ["용도", "인터넷 직접 통신", "내부·NAT 통신"],
    ["배치 예", "웹 서버, 로드밸런서", "DB, 앱 서버"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  subgraph pub [Public]
    P([인스턴스])
    P --> IGW[IGW]
    IGW --> INET[인터넷]
  end
  subgraph priv [Private]
    Q([인스턴스])
    Q --> NAT[NAT GW]
    NAT --> INET
  end`}
/>
