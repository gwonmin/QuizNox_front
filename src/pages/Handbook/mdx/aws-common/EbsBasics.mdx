import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="EBS는 EC2 인스턴스에 붙이는 블록 스토리지입니다. 볼륨 타입(gp3, io2 등)으로 성능·비용을 선택하고, 스냅샷으로 백업·다른 리전/AZ 복제를 합니다."
  bullets={[
    "볼륨 타입: gp3/gp2(범용 SSD), io2/io1(고IOPS), st1/sc1(HDD 처리량·저비용).",
    "스냅샷: 특정 시점 볼륨 복사본, S3에 저장. 다른 AZ·리전에 새 볼륨 생성 가능(복구·마이그레이션).",
    "AMI 생성 시 루트 볼륨 스냅샷 사용.",
  ]}
/>

## 볼륨 타입

<KeyTable
  columns={["타입", "용도", "특성"]}
  rows={[
    ["gp3 / gp2", "범용", "SSD, 대부분 워크로드"],
    ["io2 / io1", "고IOPS", "DB 등 저지연 필요"],
    ["st1 / sc1", "처리량·저비용", "HDD, 로그·빅데이터"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  EC2[EC2 인스턴스] <--> VOL[EBS 볼륨]
  VOL --> SNAP[스냅샷]
  SNAP --> NEW_VOL[새 볼륨 다른 AZ 또는 리전]`}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["EBS", "EC2에 연결하는 블록 스토리지"],
    ["타입", "gp3(범용), io2(고IOPS), st1/sc1(HDD)"],
    ["스냅샷", "시점 복사본, 백업·복제·AMI에 사용"],
  ]}
/>
