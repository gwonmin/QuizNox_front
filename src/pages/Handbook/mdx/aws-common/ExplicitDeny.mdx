import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="IAM 정책 평가에서 명시적 Deny가 Allow보다 우선합니다. 한 정책이라도 Effect: Deny면 해당 액션은 거부되며, 이 버킷만 제외 같은 예외를 Deny로 표현할 때 씁니다."
  bullets={[
    "Effect: Deny가 어느 정책에든 있으면 해당 액션은 거부. Allow가 여러 개 있어도 Deny 하나면 거부.",
    "정책이 없으면 기본값은 거부(Deny by default).",
    "권한 제한: 이 역할은 S3는 되지만 이 버킷만 불가처럼 예외를 Deny로 명시. 실수로 넓은 Allow를 줘도 Deny로 구간을 막을 수 있음.",
  ]}
/>

## 우선순위

<KeyTable
  columns={["우선순위", "설명"]}
  rows={[
    ["1", "Explicit Deny 있으면 거부"],
    ["2", "Explicit Allow 있으면 허용"],
    ["3", "둘 다 없으면 거부(기본값)"],
  ]}
/>
