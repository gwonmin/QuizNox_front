import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="STS는 임시 보안 자격 증명을 발급하는 AWS 서비스이고, AssumeRole은 다른 IAM 역할을 맡아서 그 역할의 권한으로 동작하는 방식입니다. 크로스 계정·서비스 위임에 사용합니다."
  bullets={[
    "STS: 임시 자격 증명(Access Key, Secret Key, Session Token) 발급. 만료 시간이 있어 장기 보관 키보다 안전. AssumeRole, GetSessionToken 등으로 발급.",
    "AssumeRole: 다른 IAM 역할을 맡아서 그 역할의 권한으로 동작. STS가 해당 역할의 임시 자격 증명을 반환.",
    "크로스 계정·서비스 간 위임에 사용.",
  ]}
/>

## 요약

<KeyTable
  columns={["구분", "설명"]}
  rows={[
    ["STS", "임시 자격 증명 발급"],
    ["AssumeRole", "다른 역할로 전환 후 임시 자격 증명 사용"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`sequenceDiagram
  participant C as 호출자
  participant STS as STS
  participant R as IAM 역할
  C->>STS: AssumeRole 역할 지정
  STS->>R: 역할 권한 확인
  R-->>STS: 임시 자격 증명 발급
  STS-->>C: Access Key, Secret Key, Session Token
  C->>C: 만료 시점까지 해당 권한으로 API 호출`}
/>
