import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="요청이 들어오면 명시적 Deny가 있으면 거부, 없으면 명시적 Allow가 있으면 허용, 둘 다 없으면 거부됩니다. IAM에서 권한 허용/거부는 이 정책 평가 로직에 따라 결정됩니다."
  bullets={[
    "기본적으로 거부(Deny by default). 명시적 Deny가 있으면 거부. 명시적 Allow가 있으면 허용. Allow/Deny 모두 없으면 거부.",
    "Explicit Deny 우선: 어떤 정책에서든 Effect: Deny가 있으면 해당 액션은 거부.",
    "Identity-based와 Resource-based 정책이 모두 적용되며, 둘 중 하나라도 Deny면 거부.",
  ]}
/>

## 평가 순서

<KeyTable
  columns={["순서", "조건", "결과"]}
  rows={[
    ["1", "명시적 Deny 있음", "거부"],
    ["2", "명시적 Deny 없음 + Allow 있음", "허용"],
    ["3", "둘 다 없음", "거부(Deny by default)"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart TD
  A[요청] --> B{명시적 Deny 있음}
  B -->|Yes| C[거부]
  B -->|No| D{명시적 Allow 있음}
  D -->|Yes| E[허용]
  D -->|No| F[거부]`}
/>
