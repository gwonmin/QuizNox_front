import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="NACL(Network ACL)은 서브넷 단위의 상태 비유지(Stateless) 방화벽입니다. 인바운드만 허용해도 응답 아웃바운드는 별도 규칙으로 허용해야 하며, Allow·Deny 규칙 모두 사용 가능합니다."
  bullets={[
    "서브넷에 연결, 서브넷 경계에서 트래픽 필터링.",
    "Stateless: 인바운드 허용해도 응답 아웃바운드는 별도 규칙으로 허용해야 함.",
    "Allow + Deny 규칙 모두 사용 가능. 규칙 번호가 작은 것부터 적용, 첫 매칭 시 적용 후 종료.",
  ]}
/>

## NACL vs Security Group

<KeyTable
  columns={["구분", "NACL", "Security Group"]}
  rows={[
    ["단위", "서브넷", "ENI(인스턴스)"],
    ["상태", "Stateless", "Stateful"],
    ["거부 규칙", "있음", "없음(Allow만)"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  subgraph in [인바운드]
    A[트래픽] --> B[NACL 규칙]
    B --> C[허용 시 서브넷 진입]
  end
  subgraph out [아웃바운드]
    D[응답 트래픽] --> E[NACL 규칙 별도 필요]
    E --> F[허용 규칙 없으면 거부]
  end`}
/>
