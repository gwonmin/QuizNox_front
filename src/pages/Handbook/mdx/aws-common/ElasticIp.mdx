import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Elastic IP(EIP)는 퍼블릭 IP를 고정해서 인스턴스 재시작·교체 후에도 같은 주소를 유지하게 하는 AWS 리소스입니다. 인스턴스에 붙였다가 해제하면 그때부터 과금되므로 사용 중일 때만 붙여 두는 것이 좋습니다."
  bullets={[
    "고정 퍼블릭 IP: 인스턴스 중지·재시작해도 유지(일반 퍼블릭 IP는 바뀔 수 있음).",
    "한 번에 한 인스턴스에만 연결. 옮기려면 해제 후 다른 인스턴스에 연결.",
    "미사용 시 과금: 인스턴스에 붙어 있지 않은 EIP는 시간당 요금 발생.",
  ]}
/>

## 용도·요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["EIP", "퍼블릭 IP 고정 리소스"],
    ["연결", "EC2(또는 NAT GW) 1대당 1개"],
    ["비용", "미연결 상태로 두면 과금"],
    ["용도", "고정 IP 필요(화이트리스트), NAT GW 퍼블릭 IP, 장애 시 같은 IP로 재연결"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  EIP[Elastic IP] <--> EC2[EC2 인스턴스]
  EIP -.->|해제 시 과금| X[미연결]`}
/>
