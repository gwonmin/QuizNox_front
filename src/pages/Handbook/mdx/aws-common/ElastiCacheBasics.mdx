import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="ElastiCache는 관리형 인메모리 캐시 서비스입니다. Redis와 Memcached 엔진 중 하나를 골라, DB·API 앞단에 캐시 계층을 두어 응답 속도·DB 부하를 줄일 때 씁니다."
  bullets={[
    "Redis: 키·값 + 리스트·셋·Pub/Sub 등. 클러스터·복제·persistence 옵션. 세션·캐시·실시간·Pub/Sub에 적합.",
    "Memcached: 단순 키·값 캐시, 다중 노드 수평 확장. 복제·persistence 없음. 단순 캐시·세션에 적합.",
    "캐시 미스 시 DB 조회 후 캐시에 적재, 이후 응답 속도·DB 부하 감소.",
  ]}
/>

## Redis vs Memcached

<KeyTable
  columns={["구분", "Redis", "Memcached"]}
  rows={[
    ["데이터 구조", "키·값 + 리스트·셋·Pub/Sub 등", "키·값만"],
    ["복제·Persistence", "지원(옵션)", "없음"],
    ["용도", "세션·캐시·실시간·Pub/Sub", "단순 캐시"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  APP[애플리케이션] --> CACHE[ElastiCache]
  CACHE --> |캐시 미스| DB[(RDS 등)]
  DB --> CACHE
  CACHE --> APP`}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["ElastiCache", "관리형 인메모리 캐시(Redis 또는 Memcached)"],
    ["Redis", "기능 풍부·복제·persistence"],
    ["Memcached", "단순·저지연·수평 확장"],
  ]}
/>
