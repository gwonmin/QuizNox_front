import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Strong Consistency는 쓰기 후 어디서 읽어도 항상 최신 값을 보장하고, Eventual Consistency는 시간이 지나면서 결국 같은 값으로 수렴합니다."
  bullets={[
    "Strong: 쓰기 직후 어떤 노드에서 읽어도 최신 값이 보인다.",
    "Eventual: 쓰기 후 잠시 동안은 이전 값이 보일 수 있지만, 복제가 전파되면 결국 수렴한다.",
  ]}
/>

## Strong vs Eventual 한눈에 비교

<KeyTable
  columns={["구분", "Strong Consistency", "Eventual Consistency"]}
  rows={[
    [
      "읽기 시점",
      "쓰기 직후 어떤 노드에서 읽어도 항상 최신 값을 본다.",
      "복제가 전파된 후에야 최신 값이 보인다.",
    ],
    [
      "쓰기 후 상태",
      "즉시 모든 노드에 반영되도록 동기 복제 또는 합의를 거친다.",
      "일부 노드는 일정 시간 동안 이전 값을 유지할 수 있다.",
    ],
    [
      "트레이드오프",
      "쓰기 지연·가용성 희생(네트워크 분할 시 대기·거절 가능).",
      "읽기 시 구식 값 가능하지만, 쓰기 지연이 작고 가용성·확장성이 좋다.",
    ],
  ]}
/>

## Strong Consistency 시나리오

<MermaidDiagram
  code={`sequenceDiagram
  participant C as Client
  participant N1 as 노드1
  participant N2 as 노드2
  C->>N1: 쓰기
  N1->>N2: 동기 복제
  N1-->>C: 성공`}
/>

## Eventual Consistency 시나리오

<MermaidDiagram
  code={`sequenceDiagram
  participant C as Client
  participant N1 as 노드1
  participant N2 as 노드2
  C->>N1: 쓰기
  N1-->>C: 성공
  N1->>N2: 비동기 복제`}
/>

## 실제 예시

<KeyTable
  columns={["구분", "예시", "이유"]}
  rows={[
    [
      "Strong",
      "잔액 조회, 재고 차감",
      "“한 번 읽은 값이 곧바로 최신”이어야 하며, 잘못된 잔액·재고를 보여주는 건 허용하기 어렵다.",
    ],
    [
      "Eventual",
      "좋아요 수, 조회수, 타임라인",
      "잠깐 숫자가 어긋나도 괜찮고, 곧 맞춰지면 되는 경우로, 읽기 부하 분산과 쓰기 속도를 우선한다.",
    ],
  ]}
/>

