import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="HTTP는 평문 애플리케이션 프로토콜이고, HTTPS는 HTTP 위에 TLS를 씌워 전송 구간을 암호화한 형태입니다."
  bullets={[
    "HTTP: 요청/응답 구조지만 평문 전송이라 도청·변조에 취약하다.",
    "HTTPS: TLS handshake로 키를 합의한 뒤, 이후 HTTP 트래픽을 암호화해 전송한다.",
  ]}
/>

## HTTP vs HTTPS 한눈에 비교

<KeyTable
  columns={["구분", "HTTP", "HTTPS"]}
  rows={[
    [
      "암호화",
      "없음(평문 전송)",
      "TLS로 전송 구간이 암호화된다.",
    ],
    [
      "구성",
      "애플리케이션 프로토콜만 사용",
      "HTTP 위에 TLS 계층을 추가한 형태",
    ],
    [
      "보안",
      "도청·변조 가능",
      "도청·변조를 어렵게 만들고 서버 인증을 제공한다.",
    ],
  ]}
/>

## HTTP 기본 개념

<KeyTable
  columns={["항목", "내용"]}
  rows={[
    [
      "요청/응답",
      "클라이언트가 요청을 보내고 서버가 응답을 돌려주는 구조이다.",
    ],
    [
      "구성 요소",
      "요청 메서드(GET, POST 등), 경로, 헤더, 본문으로 통신한다.",
    ],
  ]}
/>

## TLS handshake 개요

<KeyTable
  columns={["단계", "역할"]}
  rows={[
    [
      "ClientHello",
      "클라이언트가 지원하는 암호 스위트·TLS 버전 등을 서버에 제안한다.",
    ],
    [
      "ServerHello + 인증서",
      "서버가 사용할 암호 스위트를 선택하고 인증서를 보내 신원을 증명한다.",
    ],
    [
      "키 교환",
      "양측이 세션 키를 합의하고 이후 트래픽은 해당 키로 암호화된다.",
    ],
  ]}
/>

## TLS handshake 흐름 도식

<MermaidDiagram
  code={`sequenceDiagram
  participant C as 클라이언트
  participant S as 서버
  C->>S: ClientHello
  S->>C: ServerHello, 인증서
  C->>S: 키 교환
  %% 이후 암호화된 HTTP 통신`}
/>

