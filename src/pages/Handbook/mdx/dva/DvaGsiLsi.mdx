import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="다른 키로 조회할 수 있게 하는 인덱스입니다. 접근 패턴별로 파티션 키·정렬 키를 다르게 두려면 GSI; 같은 파티션 키에 정렬 키만 추가하려면 LSI를 사용합니다."
  bullets={[
    "GSI(Global Secondary Index): 테이블과 다른 파티션 키·정렬 키 가능. 비동기 복제. 테이블 생성 후 추가 가능",
    "LSI(Local Secondary Index): 같은 파티션 키, 다른 정렬 키만. 테이블 생성 시에만 추가 가능",
    "Query: 파티션 키(필수) + 정렬 키 조건(선택). Scan은 테이블 전체 스캔 → 가능하면 Query·GSI/LSI 사용",
  ]}
/>

## 시나리오

<KeyTable
  columns={["조건", "선택"]}
  rows={[
    ["productId·rating 등 다른 속성으로 조회", "GSI: 해당 속성을 파티션 키(또는 정렬 키)로 설정"],
    ["제품별 최고 평점 리뷰 상위 N개", "GSI: partition key = productId, sort key = rating; Query + ScanIndexForward=false"],
    ["customer_type + email_address 부분 일치", "GSI: partition key = customer_type, sort key = email_address; Query에 begins_with"],
    ["테이블은 이미 있고, 새 조회 패턴만 추가", "GSI 추가(LSI는 생성 시에만 가능)"],
    ["파티션 키는 그대로, 정렬 키만 다르게 범위 쿼리", "LSI(테이블 생성 시 정의)"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["productId·rating 등 다른 속성으로 조회", "GSI: 해당 속성을 파티션 키·정렬 키로 설정."],
    ["제품별 최고 평점 리뷰 상위 N개", "GSI: partition key = productId, sort key = rating; Query + ScanIndexForward=false."],
    ["customer_type + email_address 부분 일치", "GSI: partition key = customer_type, sort key = email_address; Query에 begins_with."],
    ["테이블 이미 있음·새 조회 패턴만 추가", "GSI 추가(LSI는 테이블 생성 시에만 가능)."],
    ["Scan 느림·다른 키로 조회 필요", "GSI 추가 후 Query 사용."],
    ["Query 결과를 정렬 키 내림차순으로", "ScanIndexForward=false 지정."],
  ]}
/>

## 요약

<KeyTable
  columns={["구분", "GSI", "LSI"]}
  rows={[
    ["파티션 키", "테이블과 다를 수 있음", "테이블과 동일"],
    ["추가 시점", "나중에 추가 가능", "테이블 생성 시만"],
    ["조건", "다른 키·새 조회 패턴 → GSI / 같은 PK, 다른 정렬 → LSI(생성 시)"],
  ]}
/>
