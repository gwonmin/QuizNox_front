import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="한 메시지를 여러 구독자에게 동시에 전달하는 패턴입니다. 필터 정책으로 구독자별로 받을 메시지를 제한할 수 있습니다."
  bullets={[
    "동작: SNS 토픽에 여러 구독(SQS, Lambda, HTTP, 이메일 등) 등록. Publish 한 번에 모든 구독자에게 전달(Fan-out)",
    "필터 정책(Filter Policy): 구독별로 메시지 속성 조건 지정. 조건을 만족하는 메시지만 해당 구독자에게 전달",
    "FIFO 토픽: 순서 보장·중복 제거; 메시지 그룹 ID 필요",
  ]}
/>

## 시나리오

<KeyTable
  columns={["조건", "선택"]}
  rows={[
    ["한 이벤트를 여러 Lambda·SQS에 동시 전달", "SNS 토픽 → 구독으로 Lambda·SQS 여러 개"],
    ["파트너별로 자기 주문만 알림", "단일 SNS 토픽 + 구독별 필터 정책(예: partnerId = 해당 파트너)"],
    ["이메일 + Lambda 동시에", "같은 토픽에 이메일 구독 + Lambda 구독"],
    ["순서·중복 제어 필요", "SNS FIFO 토픽 + 메시지 그룹 ID"],
    ["특정 속성만 받는 Lambda", "해당 Lambda 구독에 필터 정책 설정(예: eventType = email_change)"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["파트너별로 자기 주문만 알림", "단일 SNS 토픽 + 구독별 필터 정책(예: partnerId = 해당 파트너); 새 파트너는 구독·필터만 추가."],
    ["한 이벤트를 여러 Lambda·SQS에 동시 전달", "SNS 토픽에 Lambda·SQS 구독 여러 개(Fan-out)."],
    ["이메일 + Lambda 동시 알림", "같은 토픽에 이메일 구독 + Lambda 구독."],
    ["순서·중복 제어 필요", "SNS FIFO 토픽 + 메시지 그룹 ID."],
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["Fan-out", "1 메시지 → N 구독자 동시 전달"],
    ["필터 정책", "구독별로 메시지 속성 조건 → 선택적 수신"],
    ["조건", "파트너별·속성별 수신 → 필터 정책 / 순서 보장 → FIFO 토픽"],
  ]}
/>
