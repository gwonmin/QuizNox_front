import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="테스트는 빌드 결과물이 요구 사항을 만족하는지 자동으로 검증하는 단계입니다. 기능·품질 검증에 더해, DevSecOps에서는 테스트 직후 보안 스캔(SAST·의존성·이미지·시크릿)을 파이프라인에 넣어 통과한 것만 아티팩트·배포로 이어지게 한다."
  bullets={[
    "단위·통합·E2E·정적 분석 등 테스트 유형을 조합해 품질을 검증하고, 회귀를 방지한다.",
    "테스트 통과를 배포 조건으로 두고, 보안 스캔까지 통과한 결과만 아티팩트로 푸시·배포되게 하면 품질·보안 하한을 함께 보장할 수 있다.",
    "실패 시 원인 파악·수정 후 재실행이 반복되므로, 테스트 속도·안정성(flaky 제거)이 중요하다.",
  ]}
/>

## 테스트 유형

<KeyTable
  columns={["유형", "범위", "특징", "예시"]}
  rows={[
    ["단위(Unit)", "함수·클래스 단위. 의존성은 모킹.", "빠르고 많게. 회귀 탐지의 1차선.", "Jest, pytest, JUnit."],
    ["통합(Integration)", "여러 모듈·DB·API 연동.", "실제 의존성과의 동작 검증.", "API 테스트, DB 연동 테스트."],
    ["E2E(End-to-End)", "사용자 시나리오 전체. UI·네트워크 포함.", "실제 환경에 가깝지만 느리고 불안정하기 쉬움.", "Playwright, Cypress, Selenium."],
    ["정적 분석(SAST)", "코드 스타일·보안·취약점. 실행 없이 분석. DevSecOps에서 품질과 보안 검증에 함께 사용.", "ESLint, SonarQube, Semgrep, CodeQL."],
  ]}
/>

## 테스트 피라미드

<KeyTable
  columns={["계층", "비율·역할", "의도"]}
  rows={[
    ["하단: 단위", "많고 빠름. 대부분의 버그를 여기서 잡는다.", "빠른 피드백, 낮은 비용."],
    ["중간: 통합", "적당한 수. API·DB·외부 연동 검증.", "실제 동작에 가까운 검증."],
    ["상단: E2E", "소수. 핵심 시나리오만. 느리고 유지보수 비용이 큼.", "중요 플로우의 최종 확인."],
  ]}
/>

## CI에서의 테스트

- **실패 시 동작:** 테스트가 하나라도 실패하면 보통 파이프라인을 실패로 처리하고, 배포 단계로 넘기지 않는다.
- **DevSecOps:** 테스트 다음에 보안 스캔(의존성·이미지·시크릿 검출 등)을 두고, 둘 다 통과한 경우에만 아티팩트를 푸시·배포하도록 하면 취약한 버전이 나가는 것을 막을 수 있다.
- **Flaky 테스트:** 가끔만 실패하는 테스트는 신뢰를 떨어뜨리므로, 원인 제거(타이밍·순서·공유 상태) 또는 일시 스킵 후 수정하는 것이 좋다.
- **병렬화:** 테스트 수가 많으면 스위트를 나누어 병렬 실행해 전체 소요 시간을 줄인다.

---

## TDD (Test-Driven Development)

TDD는 **테스트를 먼저 작성**하고, 그 테스트를 통과시키는 최소한의 코드를 구현한 뒤 리팩터링하는 개발 방법론이다.

### Red → Green → Refactor 사이클

<MermaidDiagram
  code={`flowchart LR
  R["🔴 Red\n실패하는 테스트 작성"]
  G["🟢 Green\n테스트를 통과하는\n최소 코드 구현"]
  B["🔵 Refactor\n코드 정리\n(테스트는 계속 통과)"]
  R --> G --> B --> R`}
/>

<KeyTable
  columns={["단계", "행동", "핵심 원칙"]}
  rows={[
    [
      "Red",
      "아직 존재하지 않는 기능에 대한 테스트를 먼저 작성한다.",
      "테스트가 실패하는 것을 반드시 확인한다. 실패 없이 통과하면 테스트가 잘못된 것이다.",
    ],
    [
      "Green",
      "테스트를 통과시키는 가장 간단한 코드를 작성한다.",
      "완벽한 설계가 아니어도 된다. '동작하는 코드'가 우선이다.",
    ],
    [
      "Refactor",
      "중복 제거, 네이밍 개선, 구조 정리 등을 수행한다.",
      "테스트가 여전히 통과하는지 확인하면서 안전하게 개선한다.",
    ],
  ]}
/>

### TDD의 이점

<KeyTable
  columns={["이점", "설명"]}
  rows={[
    [
      "설계 품질 향상",
      "테스트를 먼저 작성하면 인터페이스와 의존성을 미리 고민하게 되어 결합도가 낮고 테스트하기 쉬운 구조가 된다.",
    ],
    [
      "회귀 방지",
      "모든 기능에 테스트가 존재하므로 수정 시 기존 기능이 깨지면 즉시 탐지된다.",
    ],
    [
      "리팩터링 안전망",
      "테스트 스위트가 안전망 역할을 하여 자신감 있게 코드를 개선할 수 있다.",
    ],
    [
      "문서 역할",
      "테스트 코드 자체가 '이 함수는 이런 입력에 이런 결과를 반환해야 한다'는 명세가 된다.",
    ],
    [
      "디버깅 시간 단축",
      "작은 단위로 테스트-구현을 반복하므로 버그 발생 범위가 좁아 원인을 빠르게 찾는다.",
    ],
  ]}
/>

### TDD vs 전통적 개발 프로세스

<MermaidDiagram
  code={`flowchart TB
  subgraph Traditional["전통적 개발"]
    direction LR
    T1["요구사항 분석"] --> T2["설계"] --> T3["구현"] --> T4["테스트 작성"] --> T5["버그 발견 · 수정"]
  end

  subgraph TDD["TDD"]
    direction LR
    D1["요구사항 분석"] --> D2["테스트 작성\n(기대 동작 정의)"] --> D3["최소 구현"] --> D4["리팩터링"] --> D5["다음 기능 반복"]
  end`}
/>

<KeyTable
  columns={["비교 항목", "전통적 개발", "TDD"]}
  rows={[
    [
      "테스트 작성 시점",
      "구현 이후 (또는 생략)",
      "구현 이전",
    ],
    [
      "설계 피드백",
      "구현 후 테스트 시 뒤늦게 발견",
      "테스트 작성 시 즉시 발견",
    ],
    [
      "테스트 커버리지",
      "시간 부족으로 낮아지기 쉬움",
      "자연스럽게 높은 커버리지 확보",
    ],
    [
      "리팩터링 빈도",
      "테스트 부족으로 리팩터링을 꺼리게 됨",
      "안전망이 있어 적극적으로 리팩터링",
    ],
    [
      "초기 속도",
      "빠름 (테스트 없이 바로 구현)",
      "느림 (테스트 먼저 작성)",
    ],
    [
      "장기 유지보수",
      "기술 부채 누적, 수정 비용 증가",
      "지속적 품질 유지, 수정 비용 안정",
    ],
  ]}
/>

### TDD 실천 팁

<KeyTable
  columns={["팁", "설명"]}
  rows={[
    [
      "작은 단위로 반복",
      "한 번에 하나의 동작만 테스트한다. 사이클이 짧을수록 효과적이다.",
    ],
    [
      "테스트 이름에 의도를 담기",
      "'test1' 대신 'should return 0 when cart is empty'처럼 기대 동작을 명확히 기술한다.",
    ],
    [
      "과도한 구현 금지",
      "Green 단계에서는 테스트를 통과하는 최소한만 작성한다. 미래를 예측해 코드를 추가하지 않는다(YAGNI).",
    ],
    [
      "외부 의존성은 모킹",
      "DB, API 등 외부 의존성은 Mock/Stub으로 격리하여 단위 테스트 속도와 안정성을 확보한다.",
    ],
    [
      "CI와 연동",
      "TDD로 작성된 테스트를 CI 파이프라인에 포함시켜 PR마다 자동 검증되게 한다.",
    ],
  ]}
/>
