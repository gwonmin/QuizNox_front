import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Block, File, Object는 저장소를 다루는 세 가지 기본 모델로, 단위·접근 방식·구조·대표 서비스가 다릅니다."
  bullets={[
    "Block: 고정 크기 블록 단위, 블록 주소로 접근 (EBS, 로컬 디스크 등)",
    "File: 파일/디렉터리 단위, 경로로 접근 (EFS, FSx, NFS 등)",
    "Object: 객체(ID + 데이터 + 메타데이터), ID/HTTP API로 접근 (S3 등)",
  ]}
/>

## Block vs File vs Object 한눈에 비교

<KeyTable
  columns={["구분", "Block", "File", "Object"]}
  rows={[
    [
      "단위",
      "고정 크기 블록",
      "파일·디렉터리",
      "객체(데이터 + 메타데이터 + ID)",
    ],
    [
      "접근",
      "블록 주소(offset)",
      "경로(이름)",
      "ID·HTTP API",
    ],
    [
      "구조",
      "OS가 파일 시스템으로 구성",
      "계층 구조(폴더/파일)",
      "평평한 네임스페이스",
    ],
    [
      "특징",
      "낮은 지연, 고 IOPS",
      "공유·락·풍부한 메타데이터",
      "스케일 아웃, eventually consistent",
    ],
    [
      "예시",
      "EBS, 로컬 디스크",
      "EFS, FSx, NFS",
      "S3",
    ],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  subgraph block [Block]
    B["블록 주소로 접근"]
  end
  subgraph file [File]
    F["경로로 접근 /dir/file.txt"]
  end
  subgraph obj [Object]
    O["객체 ID로 접근 bucket/key"]
  end`}
/>

