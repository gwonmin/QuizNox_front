import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="컴퓨트 비용은 서버·가상 머신·컨테이너·함수 실행 등 연산 리소스를 사용할 때 발생합니다. 사용 시간·vCPU·메모리·동시 실행 수 등에 따라 과금되며, FinOps에서는 사용량 가시화와 구매 옵션(온디맨드·예약·스팟) 선택으로 비용을 관리합니다."
  bullets={[
    "과금 단위: 사용 시간(초·시간), vCPU·메모리 크기, 동시 실행 수 등.",
    "구매 옵션에 따라 단가가 달라짐: 온디맨드(표준 단가), 예약(할인), 스팟(중단 가능 시 할인).",
    "적정 사이징: 사용률이 낮으면 작은 인스턴스·오토 스케일로 조정해 비용 절감.",
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["발생 요인", "사용 시간, vCPU·메모리, 동시 실행 수"],
    ["관리 포인트", "구매 옵션 선택, 적정 사이징, 사용량 모니터링"],
  ]}
/>
