import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="부하 테스트는 시스템이 실제 트래픽에서 어떻게 동작하는지 사전에 검증하는 SRE·운영 핵심 활동이다."
  bullets={[
    "Load Test: 예상 트래픽 수준에서 시스템의 정상 동작을 확인한다.",
    "Stress Test: 한계점 이상까지 부하를 올려 어디서 먼저 깨지는지 파악한다.",
    "Spike Test: 갑작스러운 트래픽 급증에 대한 복원력을 검증한다.",
    "Soak Test: 장시간 일정 부하를 유지하여 메모리 누수·성능 저하를 발견한다.",
  ]}
/>

## 부하 테스트가 SRE에 포함되는 이유

<KeyTable
  columns={["SRE 원칙", "부하 테스트와의 관계"]}
  rows={[
    [
      "SLO 기반 운영",
      "부하 테스트로 SLO(가용성·지연시간) 달성 여부를 사전에 검증한다.",
    ],
    [
      "Error Budget",
      "한계 부하에서 에러율이 Error Budget을 초과하는 시점을 측정한다.",
    ],
    [
      "Capacity Planning",
      "테스트 결과를 바탕으로 Auto Scaling 임계치, 인스턴스 수를 결정한다.",
    ],
    [
      "Incident Prevention",
      "병목(DB 커넥션 풀, 메모리, CPU 등)을 사전에 발견하여 장애를 예방한다.",
    ],
  ]}
/>

## 테스트 유형 비교

<KeyTable
  columns={["유형", "목적", "부하 패턴", "확인 포인트"]}
  rows={[
    [
      "Load Test",
      "예상 트래픽에서 정상 동작 확인",
      "점진적 증가 → 일정 유지 → 감소",
      "응답 시간, 에러율, 처리량(TPS)",
    ],
    [
      "Stress Test",
      "시스템 한계점 및 실패 지점 파악",
      "예상 트래픽 이상까지 지속 증가",
      "어느 지점에서 에러 급증하는지, 복구 여부",
    ],
    [
      "Spike Test",
      "급격한 트래픽 급증 대응력 검증",
      "짧은 시간에 급격히 올렸다 내림",
      "Auto Scaling 반응 속도, 요청 유실률",
    ],
    [
      "Soak Test",
      "장시간 안정성 검증",
      "일정 부하를 수 시간~수 일 유지",
      "메모리 누수, 커넥션 고갈, 성능 저하 추이",
    ],
  ]}
/>

## 부하 패턴 도식

<MermaidDiagram
  code={`graph LR
  subgraph Load["Load Test"]
    L1["시작"] --> L2["점진적 증가"]
    L2 --> L3["목표 부하 유지"]
    L3 --> L4["점진적 감소"]
  end

  subgraph Stress["Stress Test"]
    S1["시작"] --> S2["목표 이상 계속 증가"]
    S2 --> S3["한계점 도달 · 실패 관찰"]
    S3 --> S4["부하 제거 · 복구 확인"]
  end

  subgraph Spike["Spike Test"]
    K1["평상시 부하"] --> K2["순간 급증"]
    K2 --> K3["즉시 하락"]
    K3 --> K4["복원 확인"]
  end

  subgraph Soak["Soak Test"]
    O1["시작"] --> O2["일정 부하 장시간 유지"]
    O2 --> O3["성능 저하·리소스 누수 관찰"]
  end`}
/>

## 주요 도구

<KeyTable
  columns={["도구", "특징", "프로토콜"]}
  rows={[
    [
      "k6 (Grafana)",
      "JavaScript로 시나리오 작성, CLI 기반, CI/CD 파이프라인 통합이 쉽다.",
      "HTTP, WebSocket, gRPC",
    ],
    [
      "JMeter",
      "GUI 기반으로 복잡한 시나리오 구성 가능, 플러그인 생태계가 넓다.",
      "HTTP, JDBC, JMS, LDAP 등",
    ],
    [
      "Locust",
      "Python으로 시나리오 작성, 분산 실행 지원, 가볍고 유연하다.",
      "HTTP (커스텀 프로토콜 확장 가능)",
    ],
    [
      "Gatling",
      "Scala/Java 기반, 고성능 비동기 엔진, 상세한 HTML 리포트 제공.",
      "HTTP, WebSocket, JMS",
    ],
    [
      "Artillery",
      "YAML/JS로 시나리오 작성, 서버리스 분산 실행(AWS Lambda), Node.js 생태계.",
      "HTTP, WebSocket, Socket.io",
    ],
  ]}
/>

## 테스트 프로세스

<MermaidDiagram
  code={`flowchart LR
  A["목표 설정\n(SLO 기준 정의)"]
  A --> B["시나리오 작성\n(사용자 행동 모델링)"]
  B --> C["기준선 측정\n(현재 성능 파악)"]
  C --> D["테스트 실행\n(Load → Stress → Spike → Soak)"]
  D --> E["결과 분석\n(병목 식별)"]
  E --> F["개선 적용\n(스케일링·코드·인프라 최적화)"]
  F --> G["재테스트\n(개선 검증)"]
  G --> D`}
/>

## 핵심 측정 지표

<KeyTable
  columns={["지표", "설명", "목표 예시"]}
  rows={[
    [
      "TPS / RPS",
      "초당 처리 트랜잭션(요청) 수. 시스템의 처리 능력을 나타낸다.",
      "API 서버 기준 1,000 RPS 이상",
    ],
    [
      "Response Time (p50/p95/p99)",
      "요청-응답 소요 시간의 백분위 분포.",
      "p99 < 500ms",
    ],
    [
      "Error Rate",
      "전체 요청 중 실패(4xx/5xx) 비율.",
      "< 0.1%",
    ],
    [
      "Concurrent Users",
      "동시에 활성 상태인 가상 사용자 수.",
      "테스트 시나리오에 따라 다름",
    ],
    [
      "CPU / Memory / DB Connection",
      "서버 리소스 사용률. 병목 지점을 파악하는 데 필수.",
      "CPU < 70%, Memory < 80%",
    ],
  ]}
/>

## AWS 환경에서의 부하 테스트 고려사항

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    [
      "Auto Scaling 검증",
      "ASG·ECS의 스케일아웃 속도가 부하 증가 속도를 따라가는지 확인한다.",
    ],
    [
      "서비스 한도(Quota)",
      "Lambda 동시실행, API Gateway 스로틀, DynamoDB RCU/WCU 등 서비스 한도에 먼저 도달할 수 있다.",
    ],
    [
      "비용 주의",
      "대규모 부하 테스트는 트래픽·컴퓨팅 비용이 발생하므로 테스트 환경을 분리하고 예산을 설정한다.",
    ],
    [
      "사전 워밍업",
      "ELB, DynamoDB On-demand 등은 사전 워밍업이 필요할 수 있다. 갑작스러운 대량 요청 시 스로틀링이 발생한다.",
    ],
    [
      "분산 부하 생성",
      "단일 인스턴스에서 충분한 부하를 만들기 어려우면 k6 Cloud, Locust 분산 모드, Artillery의 Lambda 분산 실행 등을 활용한다.",
    ],
  ]}
/>

## 요약

<KeyTable
  columns={["핵심 포인트", "설명"]}
  rows={[
    [
      "부하 테스트 = SRE 핵심 활동",
      "SLO 검증, Capacity Planning, 장애 예방에 직결된다.",
    ],
    [
      "4가지 유형을 목적에 맞게 조합",
      "Load → Stress → Spike → Soak 순서로 점진적으로 검증하는 것이 일반적이다.",
    ],
    [
      "CI/CD 파이프라인에 통합",
      "배포 전 자동 부하 테스트를 실행하여 성능 회귀를 조기에 발견한다.",
    ],
    [
      "결과 기반 개선 반복",
      "테스트 → 병목 식별 → 최적화 → 재테스트 사이클을 반복한다.",
    ],
  ]}
/>
