import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="AIOps는 AI/ML 기술을 IT 운영에 적용하여 이상 탐지, 근본 원인 분석, 자동 대응을 지능화하는 접근 방식이다."
  bullets={[
    "기존 rule-based 모니터링의 한계를 ML 기반 패턴 인식으로 극복한다.",
    "수천 건의 알람 노이즈를 줄이고, 근본 원인(RCA)까지 자동으로 추적한다.",
    "Observability(로그·메트릭·트레이스) 데이터를 ML로 분석하여 사전 예방적 운영을 가능하게 한다.",
  ]}
/>

## 기존 모니터링 vs AIOps

<KeyTable
  columns={["구분", "Rule-based 모니터링", "AIOps (ML-based)"]}
  rows={[
    [
      "이상 탐지",
      "임계치 기반 (CPU > 80% → 알람). 정적이고 예외 패턴에 대응 어려움.",
      "ML이 정상 패턴을 학습하고, 벗어난 이상치를 자동 감지. 계절성·트렌드도 반영.",
    ],
    [
      "알람 처리",
      "알람이 쏟아져 노이즈 과다. 중요도 구분이 어려움.",
      "알람을 상관관계 기반으로 그룹핑·중복 제거. 핵심 알람만 전달.",
    ],
    [
      "근본 원인 분석",
      "엔지니어가 로그·메트릭을 수동으로 추적.",
      "토폴로지·의존성 그래프를 기반으로 근본 원인을 자동 추론.",
    ],
    [
      "대응",
      "수동 대응 또는 단순 자동화 (스크립트).",
      "과거 패턴 학습 기반 자동 대응 제안 또는 자동 실행.",
    ],
    [
      "확장성",
      "시스템이 복잡해질수록 룰 관리가 기하급수적으로 증가.",
      "데이터가 많을수록 ML 정확도가 향상.",
    ],
  ]}
/>

## AIOps 핵심 기능

<MermaidDiagram
  code={`flowchart TB
  subgraph collect ["데이터 수집"]
    LOG["로그"]
    MET["메트릭"]
    TRC["트레이스"]
    EVT["이벤트·알람"]
  end

  subgraph analyze ["ML 분석"]
    AD["이상 탐지\n(Anomaly Detection)"]
    CR["상관관계 분석\n(알람 그룹핑)"]
    RCA["근본 원인 분석\n(Root Cause Analysis)"]
    PR["예측\n(Predictive)"]
  end

  subgraph act ["조치"]
    NR["노이즈 감소\n(알람 중복 제거)"]
    AUTO["자동 대응\n(Auto-remediation)"]
    REC["대응 추천\n(Runbook 제안)"]
  end

  collect --> analyze --> act`}
/>

<KeyTable
  columns={["기능", "설명", "예시"]}
  rows={[
    [
      "이상 탐지 (Anomaly Detection)",
      "정상 패턴을 학습하고, 비정상적인 변화를 자동으로 감지한다. 임계치를 수동으로 설정할 필요가 없다.",
      "평소와 다른 응답 지연 패턴 감지, 비정상 트래픽 급증 탐지",
    ],
    [
      "알람 상관관계 분석",
      "동시에 발생하는 알람들을 원인-결과 관계로 묶어 하나의 인시던트로 그룹핑한다.",
      "DB 슬로우 쿼리 → API 타임아웃 → 프론트 에러 알람 3개를 하나의 DB 이슈로 묶음",
    ],
    [
      "근본 원인 분석 (RCA)",
      "서비스 의존성 그래프와 시계열 데이터를 결합하여 장애의 근본 원인을 자동으로 추론한다.",
      "네트워크 장애 → DB 연결 실패 → API 에러 체인에서 네트워크를 근본 원인으로 식별",
    ],
    [
      "예측 분석 (Predictive)",
      "과거 데이터의 추세를 학습하여 디스크 부족, 용량 초과 등 장애를 사전에 예측한다.",
      "현재 디스크 증가율로 3일 후 용량 초과 예측 → 사전 알림",
    ],
    [
      "노이즈 감소",
      "중복·불필요한 알람을 억제하고 핵심 알람만 전달하여 알람 피로(Alert Fatigue)를 줄인다.",
      "1,000건의 알람을 5건의 인시던트로 압축",
    ],
    [
      "자동 대응 (Auto-remediation)",
      "이전에 성공적으로 수행된 대응 패턴을 학습하여 동일 유형의 장애에 자동으로 대응한다.",
      "디스크 부족 시 자동으로 로그 정리 스크립트 실행, 비정상 Pod 자동 재시작",
    ],
  ]}
/>

## AIOps와 Observability의 관계

<MermaidDiagram
  code={`flowchart LR
  subgraph observability ["Observability (데이터 기반)"]
    L["Logs\n(무슨 일이 일어났는가)"]
    M["Metrics\n(시스템 상태는 어떤가)"]
    T["Traces\n(요청 흐름은 어떤가)"]
  end

  subgraph aiops ["AIOps (지능 계층)"]
    ML["ML 엔진"]
    IN["인사이트 도출"]
    AC["자동 조치"]
    ML --> IN --> AC
  end

  observability -->|"데이터 공급"| aiops`}
/>

<KeyTable
  columns={["구분", "Observability", "AIOps"]}
  rows={[
    [
      "역할",
      "시스템 상태를 관측 가능하게 만드는 데이터 수집·저장·시각화 체계",
      "수집된 데이터를 ML로 분석하여 인사이트·자동 대응을 제공하는 지능 계층",
    ],
    [
      "관계",
      "AIOps의 데이터 소스. 양질의 Observability 없이 AIOps는 불가능.",
      "Observability의 가치를 극대화. 사람이 볼 수 없는 패턴까지 발견.",
    ],
    [
      "비유",
      "CCTV (촬영·녹화)",
      "AI 영상 분석 (이상 행동 자동 감지)",
    ],
  ]}
/>

## AIOps 도입 성숙도

<KeyTable
  columns={["단계", "수준", "특징"]}
  rows={[
    [
      "1단계",
      "데이터 통합",
      "로그·메트릭·트레이스·이벤트를 중앙에 수집·통합한다.",
    ],
    [
      "2단계",
      "지능형 알람",
      "ML 기반 이상 탐지, 알람 상관관계 분석, 노이즈 감소를 적용한다.",
    ],
    [
      "3단계",
      "자동 근본 원인 분석",
      "서비스 토폴로지 + ML로 근본 원인을 자동 추론한다.",
    ],
    [
      "4단계",
      "예측 + 자동 대응",
      "장애를 사전 예측하고, 검증된 패턴은 자동으로 대응한다. 사람은 예외 상황만 처리.",
    ],
  ]}
/>

<MermaidDiagram
  code={`flowchart LR
  S1["1단계\n데이터 통합"]
  S2["2단계\n지능형 알람"]
  S3["3단계\n자동 RCA"]
  S4["4단계\n예측 + 자동 대응"]
  S1 --> S2 --> S3 --> S4`}
/>

## 대표 AIOps 도구

<KeyTable
  columns={["도구", "특징"]}
  rows={[
    [
      "Datadog AI/ML",
      "Watchdog 엔진이 메트릭·로그·트레이스에서 이상을 자동 감지. 근본 원인까지 추적.",
    ],
    [
      "Dynatrace Davis",
      "AI 엔진이 Full-stack 토폴로지를 자동 매핑하고 근본 원인을 정확하게 식별. 코드 레벨까지 추적.",
    ],
    [
      "PagerDuty AIOps",
      "알람 노이즈를 ML로 압축하고, 과거 대응 패턴을 학습하여 적절한 대응자·Runbook을 추천.",
    ],
    [
      "AWS DevOps Guru",
      "AWS 리소스의 운영 데이터를 ML로 분석하여 이상 탐지·권고사항 제공. CloudFormation 기반 자동 활성화.",
    ],
    [
      "Splunk ITSI",
      "IT 서비스 인텔리전스. 비즈니스 서비스 관점에서 이상 탐지·예측 분석·RCA 제공.",
    ],
    [
      "Moogsoft",
      "알람 상관관계 분석에 특화. 대량 알람을 인시던트로 자동 그룹핑하여 MTTR 단축.",
    ],
  ]}
/>

## 요약

<KeyTable
  columns={["핵심 포인트", "설명"]}
  rows={[
    [
      "AIOps = Ops를 위한 AI",
      "IT 운영(모니터링·장애 대응)에 ML을 적용하여 자동화·지능화한다.",
    ],
    [
      "Observability가 선행 조건",
      "양질의 로그·메트릭·트레이스 수집 없이 AIOps는 동작할 수 없다.",
    ],
    [
      "노이즈 감소가 즉시 체감되는 효과",
      "알람 피로를 줄이고 핵심 이슈에 집중할 수 있게 하는 것이 AIOps 도입의 첫 번째 가치이다.",
    ],
    [
      "점진적 도입이 현실적",
      "데이터 통합 → 지능형 알람 → 자동 RCA → 예측/자동 대응 순서로 성숙도를 높여가는 것이 효과적이다.",
    ],
  ]}
/>
