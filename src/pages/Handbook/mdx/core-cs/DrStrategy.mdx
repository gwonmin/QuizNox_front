import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="재해 복구(DR)는 장애·재해 시 데이터와 서비스를 얼마나 빨리, 어느 수준으로 복구할지 정하고 그에 맞는 전략을 선택하는 것입니다. RPO·RTO·비용에 따라 Backup, Pilot Light, Warm Standby, Active-Active 등으로 구분합니다."
  bullets={[
    "Backup: 정기 백업·스냅샷만 있고, 장애 시 복원에 시간 소요. RTO·RPO 모두 상대적으로 느슨할 때.",
    "Pilot Light: DB·핵심 데이터만 대기 쪽에 복제해 두고, 장애 시 나머지(컴퓨트 등)를 기동해 전환.",
    "Warm Standby: 대기 리전/센터에 축소된 서비스를 상시 돌려 두고, 장애 시 스케일 업·트래픽 전환.",
    "Active-Active: 두 곳 모두 정상 트래픽 처리. 한쪽 장애 시 나머지가 이어받음. RTO 최소, 비용 최대.",
  ]}
/>

## 전략 스펙트럼

<KeyTable
  columns={["전략", "대기 환경", "RTO", "RPO", "비용"]}
  rows={[
    ["Backup", "없음(백업만 보관)", "김", "백업 주기 따라감", "낮음"],
    ["Pilot Light", "핵심만(예: DB 복제) 상시", "중간", "복제 따라감", "중간"],
    ["Warm Standby", "축소된 환경 상시 가동", "짧음", "복제", "중~높음"],
    ["Active-Active", "동일 수준 이중화·동시 서비스", "매우 짧음", "실시간 복제", "높음"],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  subgraph level [복구 수준]
    B[Backup]
    P[Pilot Light]
    W[Warm Standby]
    A[Active-Active]
  end
  B --> P --> W --> A`}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["DR 전략", "RPO·RTO 요구와 비용의 타협선을 정하는 것"],
    ["비용", "복구 속도가 빨라질수록 상시 대기 리소스가 늘어나 비용이 올라감"],
    ["BCP", "이 전략을 정한 뒤, 복구 절차·담당·테스트를 함께 정해 둠"],
  ]}
/>
