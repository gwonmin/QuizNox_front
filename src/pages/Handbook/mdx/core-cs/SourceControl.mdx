import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="소스 관리는 코드의 변경 이력을 버전으로 저장하고, 브랜치로 작업을 나누어 협업하는 기반입니다. CI/CD 파이프라인은 커밋·PR·태그·푸시 등을 트리거로 자동 빌드·테스트를 실행한다."
  bullets={[
    "버전 관리(Git 등)로 누가·언제·무엇을 바꿨는지 추적하고, 롤백·비교·병합이 가능해진다.",
    "브랜치 전략(main/develop, Git Flow, trunk-based)에 따라 머지 시점·리뷰·트리거 정책을 정한다.",
    "파이프라인은 특정 브랜치 푸시·PR 생성·태그 푸시 등 이벤트에 반응해 빌드 단계를 시작한다.",
  ]}
/>

## 버전 관리 핵심

<KeyTable
  columns={["개념", "의미", "CI/CD 연계"]}
  rows={[
    ["커밋(Commit)", "특정 시점의 변경을 고유 ID로 저장. 이력 추적·롤백의 단위.", "커밋 푸시 시 파이프라인 트리거(브랜치별 정책)."],
    ["브랜치(Branch)", "병렬 작업·기능 단위 격리. main, develop, feature/xxx 등.", "main/develop 푸시만 빌드·배포하거나, PR 머지 시 배포 등 정책 결정."],
    ["태그(Tag)", "릴리스·버전 고정용 이름(예: v1.0.0). 특정 커밋을 가리킨다.", "태그 푸시 시 프로덕션 배포 파이프라인 트리거에 자주 사용."],
    ["머지(Merge)", "한 브랜치의 변경을 다른 브랜치에 반영. 충돌 해결 후 병합.", "머지 완료 시 CI 실행·배포 단계로 이어지는 경우가 많다."],
  ]}
/>

## 브랜치 전략 예시

<KeyTable
  columns={["전략", "특징", "트리거 예시"]}
  rows={[
    ["Git Flow", "develop(개발), release(릴리스), main(배포), hotfix. 브랜치 역할이 분리됨.", "main 머지 시 프로덕션 배포, develop 푸시 시 스테이징 빌드."],
    ["Trunk-based", "main 하나 중심, 짧은 수명 브랜치만 사용. 머지 빈도가 높다.", "main 푸시마다 빌드·테스트·배포(또는 PR 머지 시)."],
    ["GitHub Flow", "main + 필요 시 브랜치. 배포는 main 기준.", "main 푸시 또는 PR 머지 시 CI/CD 실행."],
  ]}
/>

## 파이프라인 트리거

<KeyTable
  columns={["트리거", "설명", "용도"]}
  rows={[
    ["푸시(Push)", "특정 브랜치에 커밋이 푸시될 때.", "매 커밋마다 빌드·테스트로 품질 검증."],
    ["PR(Pull Request)", "PR 생성·업데이트·머지 시.", "머지 전 빌드·테스트 통과 강제, 머지 시 배포 파이프라인 시작."],
    ["태그 푸시", "v1.0.0 같은 태그가 푸시될 때.", "릴리스 버전 고정 및 프로덕션 배포 시작."],
    ["스케줄", "cron 등으로 주기 실행.", "야간 전체 빌드·회귀 테스트 등."],
  ]}
/>
