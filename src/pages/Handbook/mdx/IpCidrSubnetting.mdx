import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="IP 주소는 네트워크 부분과 호스트 부분으로 나뉘고, CIDR와 서브넷팅으로 주소 구간을 유연하게 나눕니다."
  bullets={[
    "IPv4: 32비트, IPv6: 128비트 주소 체계",
    "CIDR: IP/프리픽스 길이로 네트워크·호스트 범위를 표현",
  ]}
/>

## IP 주소 기초

<KeyTable
  columns={["종류", "형식", "특징"]}
  rows={[
    [
      "IPv4",
      "32비트, 8비트씩 10진수 (예: 192.168.1.1)",
      "네트워크 부분 + 호스트 부분으로 구성된다.",
    ],
    [
      "IPv6",
      "128비트, 16비트씩 16진수 8블록 (예: 2001:0db8::1)",
      "연속된 0은 ::로 한 번만 축약 가능하며, 프리픽스 길이(예: /64)로 네트워크/호스트를 구분한다.",
    ],
  ]}
/>

## CIDR와 프리픽스

<KeyTable
  columns={["표기", "의미", "특징"]}
  rows={[
    [
      "10.0.0.0/16",
      "앞 16비트는 네트워크, 뒤 16비트는 호스트",
      "큰 주소 블록을 나타내며, 호스트 수가 많다.",
    ],
    [
      "10.0.1.0/24",
      "앞 24비트는 네트워크, 뒤 8비트는 호스트",
      "더 작은 서브넷으로 나누어 관리할 수 있다.",
    ],
    [
      "프리픽스 길이",
      "숫자가 커질수록 호스트 부분이 줄어든다.",
      "세분화된 네트워크를 만드는 대신 호스트 수는 감소한다.",
    ],
  ]}
/>

## 서브넷팅 도식

<MermaidDiagram
  code={`flowchart LR
  A["10.0.0.0/16"] --> B["10.0.1.0/24"]
  A --> C["10.0.2.0/24"]
  A --> D["10.0.3.0/24"]`}
/>

## 프리픽스 길이와 대략적인 호스트 수

<KeyTable
  columns={["프리픽스", "호스트 수(대략)"]}
  rows={[
    ["/24", "254"],
    ["/25", "126"],
    ["/26", "62"],
    ["/28", "14"],
  ]}
/>

