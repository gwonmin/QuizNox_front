import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="Event-driven Architecture는 상태 변화를 이벤트로 발행하고, 구독자가 비동기로 반응하는 발행·구독 기반 아키텍처입니다."
  bullets={[
    "이벤트는 \"무엇이 일어났다\"라는 사실만 전달하며, 발행자는 누가 구독하는지 알 필요가 없다.",
    "구독자는 관심 이벤트를 비동기로 처리해 결합도를 낮추고, 기능을 쉽게 확장할 수 있다.",
    "이벤트 전달 수단으로 Queue / Pub/Sub를 사용하며, 멱등한 처리 설계가 중요하다.",
  ]}
/>

## 핵심 개념 한눈에 보기

<KeyTable
  columns={["구분", "의미", "예시"]}
  rows={[
    [
      "이벤트",
      "무엇이 이미 일어났는지 나타내는 과거형 메시지.",
      "\"주문이 생성됨\", \"결제가 완료됨\" 등.",
    ],
    [
      "발행자(Publisher)",
      "이벤트를 한 번 발행하고 끝내는 쪽. 누가 구독하는지는 알 필요가 없다.",
      "주문 서비스가 \"주문이 생성됨\" 이벤트를 발행.",
    ],
    [
      "구독자(Subscriber)",
      "관심 있는 이벤트를 구독해 후속 처리를 수행하는 쪽.",
      "재고 서비스, 결제 서비스, 알림 서비스, 로그/분석 서비스 등.",
    ],
    [
      "이벤트 버스",
      "발행된 이벤트를 구독자들에게 전달하는 전달 계층.",
      "메시지 브로커, Pub/Sub, 이벤트 버스 등.",
    ],
  ]}
/>

## 특징과 장점

<KeyTable
  columns={["특징", "내용"]}
  rows={[
    [
      "결합도 감소",
      "발행자는 구독자 목록을 몰라도 되므로, 새로운 구독자를 추가해도 발행자 코드를 고치지 않아도 된다.",
    ],
    [
      "비동기 처리",
      "발행자는 이벤트를 보내고 곧바로 응답을 돌려줄 수 있으며, 무거운 처리는 구독자가 나중에 처리한다.",
    ],
    [
      "확장 용이",
      "새 기능을 추가할 때, 기존 이벤트를 구독하는 새로운 소비자를 붙여 확장할 수 있다.",
    ],
  ]}
/>

## 개념 도식

<MermaidDiagram
  code={`flowchart LR
  P["주문 서비스(발행자)"] -->|"주문 생성됨 이벤트"| B["이벤트 버스"]
  B --> R["재고 서비스(구독자)"]
  B --> Pay["결제 서비스(구독자)"]
  B --> Notify["알림 서비스(구독자)"]
  B --> Log["로그/분석 서비스(구독자)"]`}
/>

## 실제 예시

<KeyTable
  columns={["단계", "예시"]}
  rows={[
    [
      "이벤트",
      "주문 서비스가 \"주문이 생성됨\" 이벤트를 발행한다(주문 ID, 상품, 수량 등 포함).",
    ],
    ["구독자 1", "재고 서비스가 이벤트를 구독해 재고를 차감한다."],
    ["구독자 2", "결제 서비스가 결제 요청을 보낸다."],
    ["구독자 3", "알림 서비스가 이메일·SMS를 발송한다."],
    ["구독자 4", "로그·분석 서비스가 이벤트를 저장·집계한다."],
  ]}
/>

## 주의·연관 개념

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    [
      "멱등성",
      "같은 이벤트가 재전달될 수 있으므로, 구독자 처리 로직은 멱등하게 설계하는 것이 안전하다.",
    ],
    [
      "전달 수단",
      "이벤트 버스/메시징은 보통 Pub/Sub 또는 Queue로 구현되며, 세부 전송·재시도 정책은 해당 시스템에 따른다.",
    ],
  ]}
/>

