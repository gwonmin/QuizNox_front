import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";
import { MermaidDiagram } from "@/components/handbook/MermaidDiagram";

<SectionSummary
  lead="RDB는 테이블(행·열)과 관계(키)로 데이터를 모델링하고, SQL·ACID·트랜잭션으로 정형 데이터와 강한 일관성을 다룹니다."
  bullets={[
    "테이블·PK·FK·스키마·뷰·저장 프로시저가 핵심 구성 요소다.",
    "ACID·트랜잭션·인덱스로 일관성과 조회 성능을 확보한다.",
    "쿼리 튜닝은 실행 계획(EXPLAIN)과 인덱스·N+1부터 본다.",
  ]}
/>

## 1. RDB를 고를 때

<KeyTable
  columns={["상황", "이유"]}
  rows={[
    ["정형·관계 명확", "테이블·키·제약으로 모델이 분명하고 JOIN·집계에 적합"],
    ["트랜잭션·일관성 중요", "이체·재고처럼 여러 연산을 원자적으로 묶어야 할 때"],
    ["복잡한 조회·보고", "SQL·인덱스로 조건·집계·정렬을 유연하게 다룰 때"],
  ]}
/>

- 수평 확장·초대용량 쓰기·스키마리스가 더 중요하면 NoSQL 등과 조합 검토.

<KeyTable
  columns={["대표 서비스"]}
  rows={[["PostgreSQL, MySQL, Aurora, RDS, SQL Server, Oracle"]]}
/>

## 2. 핵심 구성 요소

<KeyTable
  columns={["요소", "역할"]}
  rows={[
    ["테이블", "행·열 2차원 구조 (orders, users)"],
    ["기본키(PK)", "행을 유일하게 구분"],
    ["외래키(FK)", "다른 테이블 PK 참조로 관계 표현"],
    ["스키마", "컬럼·타입·제약 정의"],
    ["SQL", "선언형 질의·갱신·트랜잭션"],
    ["뷰", "쿼리로 정의된 가상 테이블, 추상화·권한 제어"],
    ["저장 프로시저", "DB에 저장된 SQL·로직 묶음, CALL로 실행"],
  ]}
/>

## 3. ACID · 트랜잭션

- 여러 연산을 **트랜잭션** 한 단위로 묶고, **ACID**로 전부 성공 아니면 전부 롤백을 보장.

<KeyTable
  columns={["속성", "의미"]}
  rows={[
    ["Atomicity", "전부 성공 시에만 반영, 하나라도 실패하면 전부 롤백"],
    ["Consistency", "제약(FK, CHECK, UNIQUE)을 만족하는 상태만 허용"],
    ["Isolation", "동시 트랜잭션 간 읽기·쓰기 수준으로 간섭 제어"],
    ["Durability", "COMMIT 후 장애가 나도 복구해 유지"],
  ]}
/>

## 4. 인덱스 · 쿼리 튜닝

- 조건 검색·정렬·JOIN이 많으면 **인덱스** 필수. 느리면 **실행 계획(EXPLAIN)**부터 원인 좁히기. 측정 후 개선이 원칙.

### 인덱스

<KeyTable
  columns={["구분", "내용"]}
  rows={[
    ["역할", "키(컬럼) 기준 행 위치를 빠르게 찾는 보조 구조. B-tree 등으로 검색 비용 감소."],
    ["효과", "WHERE·ORDER BY·JOIN에 걸면 Full scan 대신 Index scan으로 조회 단축."],
    ["주의", "갱신 대상이므로 인덱스 과다 시 쓰기 부담 증가. 자주 조회·정렬하는 컬럼 위주 설계."],
  ]}
/>

### 쿼리 튜닝 점검

<KeyTable
  columns={["점검 포인트", "확인·대응"]}
  rows={[
    ["실행 계획", "EXPLAIN(ANALYZE)으로 스캔 방식(Index vs Seq Scan)·조인 순서·비용 확인. 느린 쿼리부터 원인 좁히기."],
    ["인덱스 미사용", "WHERE·ORDER BY·JOIN에 인덱스 적용 여부. 함수·형변환 시 인덱스 깨짐 → 상수 쪽 가공 또는 함수 인덱스 검토."],
    ["Full table scan", "대형 테이블 Seq Scan 시 조건에 맞는 인덱스·복합 인덱스(조건 사용 순서) 검토."],
    ["N+1 문제", "반복 조회 대신 IN·JOIN·배치로 한 번에 조회. 쿼리 수 축소."],
    ["불필요한 데이터", "SELECT * 지양, 필요 컬럼만·LIMIT·페이징. 대용량은 스트리밍·커서 검토."],
    ["조인·서브쿼리", "비효율적 조인 순서 시 드라이빙·순서 변경, 서브쿼리 JOIN 풀기, CTE·임시 테이블로 단계 분리 검토."],
  ]}
/>

## 5. 정규화

- 같은 사실을 한 곳에만 두어 갱신·삽입·삭제 이상을 줄이는 설계.

<KeyTable
  columns={["단계", "핵심", "효과"]}
  rows={[
    ["1NF", "한 셀에 값 하나(원자 값)", "반복 그룹 제거"],
    ["2NF", "비주요 속성이 PK 전체에만 종속", "PK 일부만 쓰는 중복 제거"],
    ["3NF", "비주요 속성끼리 종속 없음", "이행 종속 제거"],
  ]}
/>

## 6. 예: 주문 스키마 ERD

- 사용자 → 주문 → 주문 항목. 1:N 관계, PK/FK로 연결.

<MermaidDiagram
  code={`erDiagram
  USERS {
    int user_id PK
    string name
    string email
  }
  ORDERS {
    int order_id PK
    int user_id FK
    date created_at
  }
  ORDER_ITEMS {
    int item_id PK
    int order_id FK
    int product_id FK
    int qty
  }
  USERS ||--o{ ORDERS : places
  ORDERS ||--o{ ORDER_ITEMS : contains`}
/>
