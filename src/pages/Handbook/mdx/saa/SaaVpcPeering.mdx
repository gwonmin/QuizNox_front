import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="두 VPC를 1:1로 연결하는 방식입니다. 크로스 계정·리전 지원하지만 비전이적이라 A-B, B-C 피어링만으로는 A-C 통신이 되지 않습니다."
  bullets={[
    "동작: 피어링 연결 생성 후 양쪽 라우팅 테이블에 상대 VPC CIDR 경로 추가",
    "비전이적: A-B, B-C 피어링해도 A-C 간 라우팅 불가",
    "제한: 두 VPC의 CIDR이 겹치면 안 됨. 리전·계정이 다를 수 있음(동일/크로스 계정·리전 지원)",
  ]}
/>

## 시나리오

<KeyTable
  columns={["조건", "선택"]}
  rows={[
    ["두 VPC만 1:1 연결", "VPC Peering"],
    ["다수 VPC·전이적 라우팅 필요", "Transit Gateway"],
    ["온프레미스와 연결", "Direct Connect / VPN"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["두 VPC 1:1 연결·CIDR 겹치지 않음", "VPC Peering; 크로스 계정·리전 가능."],
    ["다수 VPC·허브형·전이적 라우팅", "Transit Gateway."],
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["VPC Peering", "두 VPC 1:1 연결, 비전이적"],
    ["제한", "CIDR 겹침 불가"],
    ["조건", "두 VPC 연결 → Peering / 다수 VPC·허브 → Transit Gateway"],
  ]}
/>
