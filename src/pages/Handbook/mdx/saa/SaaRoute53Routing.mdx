import { SectionSummary } from "@/components/handbook/SectionSummary";
import { KeyTable } from "@/components/handbook/KeyTable";

<SectionSummary
  lead="도메인 등록·DNS 라우팅 서비스로, 가중치·지연 시간·장애 조치 등 복잡한 라우팅 정책으로 트래픽을 분기합니다."
  bullets={[
    "Simple: 단일 리소스로 라우팅",
    "Weighted: 가중치 비율로 여러 리소스에 분배(A/B, 카나리)",
    "Latency: 지연 시간 기반으로 가장 가까운 리전으로",
    "Failover: 장애 조치(Active-Passive), 헬스 체크 기반",
  ]}
/>

## 정책별 용도

<KeyTable
  columns={["정책", "용도"]}
  rows={[
    ["Simple", "단일 리소스로 라우팅"],
    ["Weighted", "가중치 비율로 분배(A/B, 카나리)"],
    ["Latency", "지연 시간 기반 가장 가까운 리전"],
    ["Failover", "장애 조치(Active-Passive), 헬스 체크 기반"],
    ["Geolocation", "사용자 위치(대륙·국가)별 라우팅"],
    ["Geoproximity", "리소스·사용자 위치 기반 라우팅"],
  ]}
/>

## 시나리오

<KeyTable
  columns={["조건", "선택"]}
  rows={[
    ["고가용성·장애 조치", "Failover로 Primary 장애 시 Secondary로 전환"],
    ["최저 지연", "Latency 기반으로 리전별 엔드포인트"],
    ["점진 전환·A/B 테스트", "Weighted로 트래픽 비율 분배"],
    ["규정(데이터 현지화)", "Geolocation으로 특정 지역만 해당 리전으로"],
  ]}
/>

## 기출빈출

<KeyTable
  columns={["시나리오", "접근"]}
  rows={[
    ["최저 지연·리전별 엔드포인트", "Latency 라우팅."],
    ["Primary 장애 시 Secondary로 전환", "Failover + 헬스 체크."],
    ["트래픽 비율 분배·카나리", "Weighted 라우팅."],
    ["사용자 위치별 라우팅", "Geolocation."],
  ]}
/>

## 요약

<KeyTable
  columns={["항목", "설명"]}
  rows={[
    ["Route 53", "DNS·도메인 등록 + 라우팅 정책"],
    ["빈출 정책", "Weighted, Latency, Failover"],
    ["조건", "최저 지연 → Latency, 장애 조치 → Failover, 비율 분배 → Weighted"],
  ]}
/>
